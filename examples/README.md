# Examples

Each private workspace will demonstrate one supported Chrome Debugger Bridge composition. Feature tickets add runnable code while keeping these examples outside the fixed public package train.

`embedded` creates an application-owned broker and client in one process, with no HTTP or WebSocket client endpoint. Its host supplies authorization, diagnostic, identifier, time, and artifact adapters.
